{
  "name": "college-gaming-club-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix college-gaming-club/client && npm install --prefix college-gaming-club/server",
    "build": "npm run build --prefix college-gaming-club/client",
    "client": "npm run dev --prefix college-gaming-club/client",
    "server": "npm run dev --prefix college-gaming-club/server"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "react-animate-on-scroll": "^2.1.9"
  }
}
